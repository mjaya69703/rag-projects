services:
  rag-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rag-knowledge-base
    restart: unless-stopped
    ports:
      - "8000:8000"
    # Wajib: salin .env.example ke .env lalu isi LLM_API_KEY/LLM_API_BASE/LLM_MODEL.
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - PERSIST_DIR=/app/data/chroma_db
      - UPLOAD_DIR=/app/uploads
      - DB_PATH=/app/data/chat.db
      - RAG_MIN_SIMILARITY=0.6
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
